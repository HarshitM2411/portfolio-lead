import type { Certification } from "@/lib/types";

/**
 * Certifications / open-source highlights — empty at launch; hide when [].
 * Content updates: append entries here only.
 */
export const certifications: Certification[] = [];
