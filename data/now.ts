import type { NowItem } from "@/lib/types";

/**
 * Now / currently exploring — signals active growth.
 * Content updates: edit texts here; empty array hides the section.
 */
export const now: NowItem[] = [
  {
    text: "Production GenAI — LLM orchestration, LangGraph agentic workflows, RAG pipelines, and LLM evals/observability (LangSmith) with a focus on cost efficiency.",
  },
];
