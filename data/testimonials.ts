import type { Testimonial } from "@/lib/types";

/**
 * Testimonials — empty at launch; section must not render when [].
 * Content updates: append quotes here only.
 */
export const testimonials: Testimonial[] = [];
