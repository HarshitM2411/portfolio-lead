# Public site URL (no trailing slash). Used for canonical URLs, sitemap, OG.
# Local: http://localhost:3000
# Production: https://your-project.vercel.app (set after Phase 9 deploy)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
